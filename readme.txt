Java
TrominoTile.java - This java file contains the actual code which generates the output,

command to compile makefile:
make
,
command to run makefile:
make run
,
command to compile the code:
javac TrominoTile.java
,
Command to Run/Execute the code:
java TrominoTile 